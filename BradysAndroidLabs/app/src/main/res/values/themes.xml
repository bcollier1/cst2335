<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BradysAndroidLab" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Primary band color. -->
        <item name="colorPrimary">@color/my_primary</item>

        <!-- Secondary band color. -->
        <item name="colorSecondary">@color/my_secondary</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/my_statusbar</item>
        
        <item name="android:textColor">@color/my_text</item>

    </style>

    <style name="Theme.BradysAndroidLab" parent="Base.Theme.BradysAndroidLab" />
</resources>